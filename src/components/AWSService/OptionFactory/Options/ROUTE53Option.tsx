import { ROUTE53Options } from '@/src/types/Services';

export const ROUTE53Option = (id: string): ROUTE53Options => {
  return { id: id };
};
export const ROUTE53OptionComponent = ({ id }: { id: string }) => {
  return <></>;
};
