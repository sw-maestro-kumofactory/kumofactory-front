import { RDSOptions } from '@/src/types/Services';

export const RDSOption = (id: string): RDSOptions => {
  return {
    id: id,
  };
};

export const RDSOptionComponent = ({ id }: { id: string }) => {
  return <></>;
};
